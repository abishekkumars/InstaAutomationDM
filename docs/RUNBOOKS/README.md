# Runbooks

Operational runbooks (incident response, on-call procedures, deployment rollback). Populated starting Phase 13 (Security hardening) / Phase 14 (Production deployment) — see `docs/IMPLEMENTATION-ROADMAP.md`.
