.tools/
node_modules/
.next/
dist/
coverage/
playwright-report/
test-results/
pnpm-lock.yaml

# Auto-regenerated by `next build`/`next dev` on every run; not hand-edited, and its
# generated formatting drifts from ours each time, causing needless format:check churn.
next-env.d.ts

# Hand-written prose with tables/links; not yet run through Prettier formatting.
# Revisit if/when we want Markdown formatting enforced in CI.
*.md
