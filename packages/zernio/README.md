# packages/zernio

`InstagramProvider` abstraction and the `ZernioInstagramProvider` implementation. Owns all direct calls to the Zernio API/SDK so `apps/api` and `apps/worker` never talk to Zernio directly. Not yet scaffolded — see [docs/ZERNIO-INTEGRATION.md](../../docs/ZERNIO-INTEGRATION.md).
